<!DOCTYPE html>
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        style
            include ./email.css
    body
        h2 Restablecer Password
        p Hola, has solicitado restablecer tu password, haz click en el siguiente enlace para reestablecerlo.
        a(href=`${resetUrl}`) Resetear el Password
        p Si no puedes acceder al enlace, visita #{resetUrl}
        p Si no solicitaste email, puedes ignorarlo