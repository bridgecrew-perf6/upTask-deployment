<!DOCTYPE html>
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        style
            include ./email.css
    body
        h2 Confirma tu cuenta UpTask
        p Ya estas casi inscrito, presiona el siguiente botón.
        a(href=`${confirmarUrl}`) Confirma tu cuenta
        p Si no puedes acceder al enlace, visita #{confirmarUrl}
        p Si no solicitaste email, puedes ignorarlo